{
  "name": "raylib-pointers",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "gen:bun": "bun tools/gen_bun.js",
    "gen:lib": "bun tools/gen_lib.js",
    "build": "cmake -G Ninja -B build && cmake --build build",
    "test": "bun test.js"
  }
}
